NODE_ENV=development
PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/shms
JWT_SECRET=mysecretkey123456789

# Comma-separated browser origins (student app, admin app). Required for production CORS.
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3001

# Optional: override default API rate limit per IP per 15 minutes
# API_RATE_LIMIT_MAX=500

CLIENT_URL=http://localhost:5173

# Razorpay — required for real payments (no fallbacks in production code paths)
RAZORPAY_KEY_ID=rzp_live_SVZQBJb3QiKHc
RAZORPAY_KEY_SECRET=fnL94zBJT4n4WwoXXno635Xn
# From Razorpay Dashboard → Webhooks (preferred over KEY_SECRET for verification)
RAZORPAY_WEBHOOK_SECRET=

CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=

# Bootstrap admin/warden for empty DB (npm run seed:root-users)
BOOTSTRAP_ADMIN_EMAIL=admin@shms.com
BOOTSTRAP_ADMIN_PASSWORD=ChangeMeAdmin!123
BOOTSTRAP_WARDEN_EMAIL=warden@shms.com
BOOTSTRAP_WARDEN_PASSWORD=ChangeMeWarden!123

# Chatbot (OpenAI-compatible API key — see geminiService.js)
OPENAI_API_KEY=your_openai_api_key
